#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Use bun from home directory if not in PATH
if command -v bun >/dev/null 2>&1; then
  BUN=bun
else
  BUN=~/.bun/bin/bun
fi

$BUN run typecheck
$BUN lint-staged
